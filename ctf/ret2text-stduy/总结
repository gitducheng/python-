# ret2text
    1、通过IDA解析二进制程序，找到想要跳转的栈地址；然后查看主进程中所调用函数的栈地址，计算两个地址的偏移量；python3的p32、p64函数转换时，需要加上“.decode("iso-8859-1")”变成字符串形式；
    2、通过IDA解析二进制程序，查找高危函数栈地址；使用gdb在高危函数栈地址打断点，拿到esp地址，加上IDA的偏移拿到s地址，再计算s与ebp的偏移；最后加上4位或8位就是与返回地址的偏移；
## 32位
    函数结束栈地址填充为4个字符；

## 64位
    函数结束栈地址填充为8个字符；